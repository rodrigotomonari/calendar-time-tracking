<%= render 'header' %>

<table class="table table-striped table-hover">
  <thead>
  <tr>
    <% if params[:group_month] %>
        <th>Ano</th>
        <th>Mês</th>
    <% end %>
    <th>Projeto</th>
    <th>Horas consumidas</th>
  </tr>
  </thead>
  <% total = 0 %>
  <% if @results.size > 0 %>
      <tbody>
      <% @results.each do |result| %>
          <% total += result[1] %>
          <tr>
            <% if params[:group_month] %>
                <td><%= result[2] %></td>
                <td><%= result[3] %></td>
            <% end %>
            <td><%= result[0] %></td>
            <td><%= result[1].to_f / 3600 %></td>
          </tr>
      <% end %>
      </tbody>
      <tfoot>
      <tr>
        <th colspan="<%= @results.first.size - 1 %>"></th>
        <th><%= total.to_f / 3600 %></th>
      </tr>
      </tfoot>
  <% end %>
</table>
